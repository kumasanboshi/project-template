# CLAUDE.md

## ⚠️ セットアップ（新規プロジェクト時に必ず更新）

このファイルの以下のセクションをプロジェクトに合わせて書き換えること：
- **Project**: プロジェクト名と説明
- **Tech Stack**: 使用技術
- **Key Commands**: 実際のコマンド
- **Project Structure**: 実際のディレクトリ構成
- **Important Files**: プロジェクトの主要ドキュメントパス
- **Domain Terms**: プロジェクト固有の用語
- **STEP 5**: 仕様書のファイルパスをプロジェクトに合わせて更新
- **STEP 6**: 型チェックコマンドをプロジェクトの言語に合わせて更新

---

## ⚠️ 一般ルール（厳守）

### 不確かな情報の扱い
- 根拠のない説明を断言しない。原因が不明な場合は「わかりません」と明示し、調査手順を提案する
- 外部サービス（料金・API仕様・プラットフォームポリシー）の情報が必要な場合、実装前にユーザーに公式ドキュメントのURLまたは内容の提供を依頼する。自分の知識で断言しない

### バグ修正時のルール
- バグ修正前に、影響するすべてのモード・コンテキストを列挙する
- 修正がすべてのケースをカバーしているか確認してからコミットする

### Issueの読み込み
- Issue番号が指定された場合、実装開始前にIssueの全文を読む。サブ要件を見落とさないこと

### データ操作
- データのインポート・挿入スクリプトを書く前に、必ずバリデーションを先に行う（重複・欠損・範囲外の値・エンコーディング異常を確認）

### Gitワークフロー
- コミット後は必ず `git push` まで実行する

### デバッグ時のルール
- デバッグ時は、コード解析に深入りする前にまず `git log` / `git diff` でgit履歴を確認する
- 過去の変更が原因であることが多く、コードをトレースするより早く根本原因が判明するケースが多い

### ビルド・マージのルール
- ビルドをトリガーする前は、必ずユーザーに確認を求める
- PRをマージする前は、必ずユーザーに確認を求める
- 明示的な承認なしに自動ビルド・自動マージは禁止

### コード標準
- 色・サイズ・文字列などの値をハードコードしない。プロジェクトの定数・テーマ・仕様に従う
- 新しいライブラリが必要な場合は、実装前にユーザーに確認する

---

## Project
<!-- プロジェクト名と簡単な説明 -->
PROJECT_NAME - 説明

## Tech Stack
<!-- 使用技術を記載 -->
- Frontend:
- Backend:
- Database:
- API:
- Testing:

## Key Commands
```bash
# よく使うコマンドを記載
npm start          # 開発サーバー起動
npm run build      # ビルド
npm test           # テスト実行
npm run lint       # リンター実行
```

## Project Structure
```
src/
├── components/    #
├── pages/         #
├── services/      #
├── hooks/         #
├── types/         #
├── utils/         #
└── constants/     #
```

## Important Files
- `docs/PROJECT.md` - プロジェクト詳細・要件
- `docs/ARCHITECTURE.md` - 技術設計・データモデル
- `docs/CONTRIBUTING.md` - コーディング規約・開発フロー

## Domain Terms
<!-- プロジェクト固有の用語を記載 -->
- **Term1**: 説明
- **Term2**: 説明

## Guidelines
- 不明点は確認してから実装
- 既存のコードスタイルを尊重
- **テスト駆動開発（TDD）を採用**

---

## ⚠️ ワークフロー厳守

**重要: 以下のルールを必ず守ること**

1. **テストが通った = 完了ではない**
   - STEP 3（実装）が終わっただけ
   - 必ず STEP 4〜8 を実行すること

2. **直接 main ブランチにプッシュしない**
   - 必ずフィーチャーブランチを作成
   - PR を作成してレビューを経てからマージ

3. **ステップをスキップしない**
   - 特に STEP 4（実装検証）と STEP 7（PRレビュー）は必須

---

## モデル使い分け

| モデル | 用途 |
|--------|------|
| **haiku** | 単純な探索・ファイル確認、高速フィードバック |
| **sonnet** | 通常の実装・テスト・検証 |
| **opus** | 複雑な設計判断、最終レビュー |

**opus を使う判断基準:**
- 仕様に曖昧な部分がある
- 複数のアーキテクチャ案のトレードオフ判断
- 最終レビューで品質を担保したい時

---

## Development Workflow（TDD）

各タスクは以下のステップで進める。
**確認事項や問題が発生しない限り、STEP 1〜8を最後まで連続で実施すること。**

### STEP 1: 探索（Exploreエージェント / haiku）

| 内容 | 詳細 |
|------|------|
| 関連ファイル調査 | 既存コード、型定義、テストの確認 |
| 仕様確認 | PROJECT.md / ARCHITECTURE.md の該当箇所を確認 |

### STEP 2: 計画（Planエージェント / sonnet or opus）

| 内容 | 詳細 |
|------|------|
| 仕様の不明点確認 | 不明点があればユーザーに確認 |
| テスト設計 | テストケースの洗い出し（正常系・異常系・境界値） |
| 実装方針策定 | インターフェース設計、依存関係の整理 |

※ 複雑な設計判断が必要な場合は **opus** を使用

**計画に含めるべき内容:**
- テストファイルのパス
- テストケース一覧（describe/it構造）
- 実装ファイルのパス
- 関数シグネチャ（入出力の型）

### STEP 3: 実装（TDDサイクル）

```
┌─────────────────────────────────────────────────────┐
│  3-1. テストを書く                                    │
│       期待する動作を明確にする                         │
│       ↓                                              │
│  3-2. テストの失敗を確認                              │
│       テストが失敗することを確認                       │
│       ↓                                              │
│  3-3. テストをコミット                                │
│       テストの内容を固定（実装前にコミット）            │
│       ↓                                              │
│  3-4. 実装を書く                                      │
│       テストが通るように実装を作成                     │
│       ⚠️ テストコードは変更しない                      │
│       ↓                                              │
│  3-5. 実装をコミット                                  │
│       完成したコードを保存                            │
└─────────────────────────────────────────────────────┘
```

### STEP 4: 実装検証（ダブルレビュー）

テストがすべて通ったら、**2つの異なるAI**でレビューを実施:

#### 4-1. Claude Code レビュー（独立サブエージェント）

| 検証項目 | 内容 |
|----------|------|
| テスト依存性 | テストに過度に依存した実装になっていないか |
| エッジケース | テストでカバーされていないケースはないか |
| コード品質 | 可読性、保守性、パフォーマンスの観点 |
| 設計原則 | SOLID原則、DRY原則への準拠 |

#### 4-2. Codex CLI レビュー

```bash
codex exec review --uncommitted
```

| 検証項目 | 内容 |
|----------|------|
| セキュリティ | 脆弱性、機密情報の漏洩リスク |
| 型安全性 | TypeScript strict mode 違反 |
| ベストプラクティス | 推奨パターンへの準拠 |

### STEP 5: 仕様書の更新確認

実装による変更が仕様書に影響するか確認し、必要なら更新する。

| 確認対象 | 更新が必要なケース |
|----------|-------------------|
| `docs/ARCHITECTURE.md` | 型定義・DBスキーマ・画面・サービス・ナビゲーション・ライブラリの変更 |
| `docs/PROJECT.md` | 新機能追加・スプリント進捗・設計判断の変更 |
| `docs/CONTRIBUTING.md` | 開発フロー・規約の変更 |

### STEP 6: PR作成（Bashエージェント）

| 内容 | 詳細 |
|------|------|
| 型チェック | TypeScriptの場合: `npx tsc --noEmit` |
| 全テスト実行 | `npm test` |
| PR作成 | テスト・実装・仕様書更新を含むPR |

### STEP 7: PRレビュー（ダブルレビュー）

#### 7-1. Claude Code レビュー（独立サブエージェント）

| 内容 | 詳細 |
|------|------|
| PRの差分確認 | 変更内容が意図通りか確認 |

#### 7-2. Codex CLI レビュー

```bash
codex exec review --base main
```

#### 7-3. 指摘対応

| 内容 | 詳細 |
|------|------|
| 指摘確認 | 各レビューの指摘を確認 |
| 修正実施 | 必要な修正を実施 |
| 再レビュー | 修正後、必要に応じて再レビュー |

#### 7-4. 承認

両方のレビューで問題がなければ承認:
```bash
gh pr review --approve
```

### STEP 8: マージ（Bashエージェント）

| 内容 | 詳細 |
|------|------|
| 最終確認 | 全レビュー指摘がクローズされていること |
| PRマージ | `gh pr merge --merge` |
| mainブランチ更新 | `git checkout main && git pull` |
| ブランチ削除 | フィーチャーブランチを削除（ローカル・リモート） |

---

## TDDのルール

1. **Red → Green → Refactor**
   - Red: 失敗するテストを書く
   - Green: テストが通る最小限の実装
   - Refactor: コードを改善（テストは変更しない）

2. **テストファースト**
   - 実装前に必ずテストを書く
   - テストが仕様書の役割を果たす

3. **テストコードの不変性**
   - 実装フェーズでテストコードを変更しない
   - テストが間違っていた場合は新しいサイクルで修正

4. **小さなステップ**
   - 一度に1つの機能をテスト→実装
   - 複雑な機能は分割して段階的に実装

---

## サブエージェント一覧

| エージェント | 用途 |
|-------------|------|
| **Plan** | コードベース探索 + 実装計画 + テスト設計 |
| **Explore** | コードベース探索（調査のみ） |
| **Bash** | git操作、コマンド実行 |
| **general-purpose** | 実装検証、複雑なマルチステップタスク |

---

## テストファイル命名規則

```
src/__tests__/
├── {機能名}.test.ts      # サービス・ユーティリティのテスト
├── {コンポーネント名}.test.tsx  # コンポーネントのテスト
└── integration/          # 統合テスト
```

## コミットメッセージ規則

```
test: {機能名}のテストを追加
feat: {機能名}を実装
fix: {バグ内容}を修正
refactor: {対象}をリファクタリング
```
